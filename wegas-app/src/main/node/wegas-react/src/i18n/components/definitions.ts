export interface ComponentsTranslations {
  inbox: {
    subject: string;
    sender: string;
    date: string;
    attachments: string;
    noSelectionMessage: string;
  };
  question: {
    validate: string;
  };
  select: {
    plzChooseValue: string;
    noChoiceInfo: string;
  };
}
