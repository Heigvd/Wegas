{{#modal-dialog action="close" title="Teams List" }}
    <ul>
        {{#each team in teams}}
            <li style="width:100%; padding-right:15px"><span style="display:inline-block;font-weight:bold">{{team.name}}</span>
                {{#if team.hasDeclaredSize}}
                    {{#if team.realMatchesDeclaredSize}}
                        <span style="position: absolute; left: 400px; color:#999;">
                            (Size: {{team.playerNames.length}})
                        </span>
                    {{else}}
                        {{#if team.realGreaterThanDeclaredSize}}
                            <span style="position: absolute; left: 400px; color:#999;">
                                (Size: {{team.declaredSize}} <span style="color:red">&lt;</span> Players: {{team.playerNames.length}})
                            </span>
                        {{else}}
                            <span style="position: absolute; left: 400px; color:#999;" >
                                (Size: {{team.declaredSize}} &gt; Players: {{team.playerNames.length}})
                            </span>
                        {{/if}}
                    {{/if}}
                {{else}}
                    <span style="position: absolute; left: 400px; color:#999;">
                        (Size: {{team.playerNames.length}}, no declared size)
                    </span>
                {{/if}}
            <ul style="margin-bottom:20px">
                {{#each player in team.playerNames}}
                    <li>{{player}}</li>
                {{else}}
                    <i>No players</i>
                {{/each}}
            </ul>
            </li>
        {{else}}
            No teams
        {{/each}}
    </ul>
{{/modal-dialog}}
